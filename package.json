{
  "name": "ecowatch",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm --filter shared build && pnpm --parallel -r run build",
    "start": "pnpm --parallel -r run start",
    "lint": "pnpm --parallel -r run lint",
    "test": "pnpm --parallel -r run test",
    "prisma:generate": "prisma generate --schema prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema prisma/schema.prisma",
    "prisma:studio": "prisma studio --schema prisma/schema.prisma",
    "env:copy:local": "cp env/.env.local apps/web/.env && cp env/.env.local apps/api-gateway/.env && cp env/.env.local apps/mqtt-influxdb-service/.env && cp env/.env.local apps/data-simulator/.env && cp env/.env.local .env",
    "env:copy:prod": "cp env/.env.prod apps/web/.env && cp env/.env.prod apps/api-gateway/.env && cp env/.env.prod apps/mqtt-influxdb-service/.env && cp env/.env.prod apps/data-simulator/.env && cp env/.env.prod .env",
    "env:clean": "rm -rf apps/web/.env && rm -rf apps/api-gateway/.env && rm -rf apps/mqtt-influxdb-service/.env && rm -rf apps/data-simulator/.env && rm -rf .env"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "@nestjs/cli": "^11.0.5",
    "@types/express": "^5.0.1",
    "@types/node": "^22.14.0",
    "eslint": "^9",
    "eslint-config-prettier": "^10.1.5",
    "eslint-plugin-prettier": "^5.2.6",
    "globals": "^16.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "@types/jest": "^29.5.12",
    "prisma": "^6.5.0",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.31.1"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/core": "^11.1.3",
    "@nestjs/platform-express": "^11.1.3",
    "@nestjs/swagger": "^11.1.3",
    "@prisma/client": "^6.5.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "bcrypt": "^5.1.1"
  },
  "pnpm": {
    "overrides": {
      "brace-expansion@>=1.0.0 <=1.1.11": ">=1.1.12",
      "brace-expansion@>=2.0.0 <=2.0.1": ">=2.0.2",
      "multer": "^2.0.2",
      "@eslint/plugin-kit": ">=0.3.4",
      "tmp": ">=0.2.4"
    }
  }
}