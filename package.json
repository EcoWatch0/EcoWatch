{
  "name": "ecowatch",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm --parallel -r run build",
    "start": "pnpm --parallel -r run start",
    "lint": "pnpm --parallel -r run lint",
    "test": "pnpm --parallel -r run test",
    "prisma:generate": "prisma generate --schema prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema prisma/schema.prisma",
    "prisma:studio": "prisma studio --schema prisma/schema.prisma",
    "env:copy:local": "cp env/.env.local apps/web/.env && cp env/.env.local apps/api-gateway/.env && cp env/.env.local apps/mqtt-influxdb-service/.env && cp env/.env.local apps/data-simulator/.env",
    "env:copy:prod": "cp env/.env.prod apps/web/.env && cp env/.env.prod apps/api-gateway/.env && cp env/.env.prod apps/mqtt-influxdb-service/.env && cp env/.env.prod apps/data-simulator/.env",
    "env:clean": "rm -rf apps/web/.env && rm -rf apps/api-gateway/.env && rm -rf apps/mqtt-influxdb-service/.env && rm -rf apps/data-simulator/.env"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@nestjs/cli": "^11.0.5",
    "@types/express": "^5.0.1",
    "@types/node": "^22.14.0",
    "eslint": "^9",
    "eslint-config-prettier": "^10.1.1",
    "eslint-plugin-prettier": "^5.2.6",
    "globals": "^16.0.0",
    "jest": "^29.7.0",
    "prisma": "^6.5.0",
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.29.0"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.12",
    "@nestjs/core": "^11.0.12",
    "@nestjs/platform-express": "^11.0.12",
    "@nestjs/swagger": "^11.0.12",
    "@prisma/client": "^6.5.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  }
}